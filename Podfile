source 'https://cdn.cocoapods.org/'

platform :ios, '14.0'
workspace 'PopupBridge.xcworkspace'

use_frameworks!

target 'UnitTests' do
  project 'PopupBridge'
  pod 'OCMock', :inhibit_warnings => true
  pod 'xcbeautify'
end
